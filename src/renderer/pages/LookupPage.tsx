import { Box, Typography } from '@mui/material';

/** Placeholder — managed series CRUD and lookup results are built in milestone M7. */
export function LookupPage(): JSX.Element {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5" gutterBottom>
        Lookup
      </Typography>
      <Typography color="text.secondary">
        Managed series and lookup results arrive in milestone M7.
      </Typography>
    </Box>
  );
}
