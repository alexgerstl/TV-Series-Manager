import { describe, expect, it } from 'vitest';

/**
 * M1.1 scaffold placeholder test.
 *
 * Confirms the Vitest pipeline (config, TS transform, test runner) is wired
 * correctly. Real unit tests start in M2.1 (Parser).
 */
describe('project scaffold', () => {
  it('runs the test pipeline successfully', () => {
    expect(true).toBe(true);
  });
});
