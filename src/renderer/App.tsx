/**
 * Placeholder root component for the M1.1 scaffold task.
 *
 * The real tab shell (Monitor / Lookup / NAS Sync / Tools / Settings / Logs)
 * is built in M1.8 per architecture.md §9 Milestone 1.
 */
export function App(): JSX.Element {
  return (
    <main style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>TV Series Manager</h1>
      <p>Scaffold running. Tab shell arrives in milestone M1.8.</p>
    </main>
  );
}
